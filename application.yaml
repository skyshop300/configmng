eureka:
  client: 
    register-with-eureka: true 
    fetch-registry: true 
    service-url:
      defaultZone: ${eureka_servers:http://localhost:8761/eureka}

ribbon:
  eureka:
    enabled: true
  MaxAutoRetries: 1                 #동일 서비스 연결시도 횟수: 최소 1번 이상으로 지정  
  MaxAutoRetriesNextServer: 1       #다음 서비스 연결 시도 횟수: 대상 서비스 수 이상으로 지정-서비스별 지정  
  OkToRetryOnAllOperations: true    #Retry 사용 여부: Failover하려면 true로 지정 
  ConnectTimeout: 3000             # 서비스별 지정 
  ReadTimeout: 3000                # 서비스별 지정 
  RetryableStatusCodes: 503, 408, 500, 404
  # 아래 설정은 eureka 미사용시에만 유효 
  #listOfServers: a.com,b.com
  #ServerListRefreshInterval: 2000
  
spring:
  rabbitmq:
    host: ${mq_host:169.56.84.41}
    port: ${mq_port:31060}
    username: ${mq_id:guest}
    password: ${mq_pw:guest}

management:
  security: 
    enabled: false
  endpoints:
    web:
      exposure:
        include: '*'
greeting: from application.yaml
